
# fu-rem

> 动态设置根字体大小的工具


## Install

```bash
npm install fu-rem
```

## Usage

```js
import setRem from 'fu-rem';

// 初始化，默认设计稿宽度为 750，根字体大小为 100px
setRem();

// 自定义设计稿宽度和根字体大小
setRem({
  designWidth: 750,
  baseSize: 100
});
```
